.venv/
__pycache__/
*.pyc
.env
data/
# ...except the small, static province boundaries bundled with the package.
!src/postcodejager/data/
!src/postcodejager/data/*.geojson
*.sqlite
.pytest_cache/
*.egg-info/
build/
dist/

# macOS
.DS_Store

# OpenTofu / Terraform — never commit state or secret vars
deploy/tofu/.terraform/
deploy/tofu/*.tfstate
deploy/tofu/*.tfstate.*
deploy/tofu/terraform.tfvars

# AI process artifacts — kept locally, never published
.impeccable/
docs/superpowers/
tasks/
PRODUCT.md
